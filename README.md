# Speedtest K8s

This repository contains the Kubernetes manifests for deploying the LibreSpeed speedtest application to the homelab cluster.

## Deployment
This repository is monitored by Flux CD and automatically deployed to the `speedtest` namespace.
